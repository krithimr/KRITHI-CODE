install.packages("lattice")
library(lattice)
delivery_time <- read.csv("C:/Users/DELL/Desktop/New folder/DATA SCIENCE/ASSIGNMENTS/COMPLETED/SIMPLE LINEAR REGRESSION St/delivery_time.csv")
summary(calories_consumed)
hist(calories_consumed$calories)
qqnorm(calories_consumed$calories)
qqline(calories_consumed$calories)
boxplot(calories_consumed)
attach(calories_consumed)
plot(calories,weight)
plot(calories,weight,main="ScatterPlot",col="Dodgerblue4",col.main="Dodgerblue4",xlab="Calories",ylab="Weight",pch=20)
cor(calories,weight)
reg<-lm(weight~calories,data=calories_consumed)
summary(reg)
confint(reg,level=0.95)
predict<-predict(reg,interval="predict")
predict
cor(predict,weight)
reg2<-lm(weight~log(calories),data=calories_consumed)
summary(reg2)
confint(reg2,level=0.95)
predict2<-predict(reg,interval="predict")
predict2
cor(predict2,weight)
reg3<-lm(weight~sqrt(calories),data=calories_consumed)
summary(reg3)
confint(reg3,level=0.95)
predict3<-predict(reg,interval="predict")
predict3
cor(predict3,weight)
